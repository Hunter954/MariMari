{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "flask --app wsgi:app db init || true && flask --app wsgi:app db migrate -m \"init\" || true && flask --app wsgi:app db upgrade && python scripts/seed.py || true && gunicorn wsgi:app --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
